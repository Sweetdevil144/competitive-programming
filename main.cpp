#include <bits/stdc++.h>
using namespace std;
// This can be used to shorten the code.
#define loop(i,a,b) for (int i = a; i <= b; i++)


int main() {
    int a=4,b=10;
    cout << a << "  " << b << '\n';
    swap(a,b);
    cout << a << "  " << b;
    return 0;
}
